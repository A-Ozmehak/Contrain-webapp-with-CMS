const TextAndSubtextBlock = ({ largerText, subtext }: { largerText: string, subtext: string }) => (
    <div>
      <h3>{largerText}</h3>
      <p>{subtext}</p>
    </div>
);

export default TextAndSubtextBlock;
